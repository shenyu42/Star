rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isSignedIn() {
      return request.auth != null;
    }

    match /users/{uid} {
      allow read: if isSignedIn();
      allow write: if isSignedIn() && request.auth.uid == uid;
    }

    match /couples/{coupleId} {
      allow read: if isSignedIn() && resource.data.memberUids.hasAny([request.auth.uid]);
      allow create: if isSignedIn();
      allow update: if isSignedIn() && resource.data.memberUids.hasAny([request.auth.uid]);
      allow delete: if isSignedIn() && resource.data.memberUids.hasAny([request.auth.uid]);
    }

    match /photos/{photoId} {
      allow read, write: if isSignedIn();
    }

    match /events/{eventId} {
      allow read, write: if isSignedIn();
    }
  }
}
